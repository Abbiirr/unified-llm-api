[project]
name = "unified-llm-api"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "litellm[proxy]>=1.82.2",
    "pyyaml>=6.0.3",
]
